server:
  port: 8080
  servlet:
    context-path: /api

spring:
  application:
    name: legal-qa-system

  datasource:
    url: jdbc:mysql://localhost:3306/legal_qa?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: hjj060618
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true

# DeepSeek API配置
deepseek:
  api:
    url: https://api.deepseek.com/v1/chat/completions
    api-key: ${DEEPSEEK_API_KEY:sk-d75528854e8641dba0f96d8878b9c678}
    model: deepseek-chat
    timeout: 120000  # 120秒，因为DeepSeek API可能响应较慢

# JWT配置
jwt:
  secret: legal-qa-system-secret-key-2024
  expiration: 86400000 # 24小时

# 文件上传配置
file:
  upload:
    path: ./uploads/
    max-size: 10MB

logging:
  level:
    root: INFO
    com.legal: DEBUG
